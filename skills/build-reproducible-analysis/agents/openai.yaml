interface:
  display_name: "Build Reproducible Analysis"
  short_description: "Design traceable, reproducible analysis"
  default_prompt: "Use $build-reproducible-analysis to design or audit this analysis for reproducibility, data integrity, and compute efficiency."
policy:
  allow_implicit_invocation: false
